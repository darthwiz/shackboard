<%smileys = Smiley.all(@user)%>
<ul>
<% smileys.each do |sm| %>
  <li>
    <%=link_to(
      image_tag(sm.url, :title => sm.code, :border => 0),
      'javascript:;',
      :onclick => "insert_text('[img]#{sm.url}[/img]', true); return false"
    )%>
  </li>
<% end %>
</ul>
