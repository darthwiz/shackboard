#fb_connect
  .title
    %h1 Accesso con Facebook
  -if @user
    .redirect
  -else
    .login
      .message
        Hai richiesto l'accesso con Facebook ma non abbiamo trovato il tuo
        utente.
      .register
        Se sei un nuovo utente, puoi registrarti ora con un solo click:
        .form
          -form_for @new_user do |f|
            =f.hidden_field :fbid
            .username
              %span.label nome utente:
              =f.text_field :username
            =f.submit 'crea nuovo utente'
        .note
          (potrai completare il tuo profilo con ulteriori dettagli in seguito)
      .alternative
        oppure
      .connect
        Se sei un utente già registrato, puoi collegare il tuo account a quello
        di Facebook. Per questo sarà necessario autenticarti:
        .form
          -form_for :existing_user, :url => fblink_users_path do |f|
            =f.hidden_field :fbid, :value => @fb_user.id
            .username
              %span.label nome utente:
              =f.text_field :username
            .password
              %span.label password:
              =f.password_field :password
            =f.submit 'collega account'
