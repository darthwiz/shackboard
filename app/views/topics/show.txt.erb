<%
host = 'www.studentibicocca.it'
-%>
Discussione: <%=cleanup(@topic.title)%> (messaggi da <%=@posts.first.seq + 1%> a <%=@posts.last.seq + 1%>)
[<%=topic_url @topic, :host => host%>]

<%@posts.each do |p|%>
--------------------------------------------------------------------------------
Inviato da <%=p.cached_user.username%> il <%=p.created_at.strftime('%d/%m/%Y')%>, <%=p.created_at.strftime('%H.%M')%>

<%=BbText.new(p.message).to_plaintext%>
<%end%>
